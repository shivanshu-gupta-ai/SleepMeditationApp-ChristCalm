# Reference only — do NOT copy secrets into local .env files.
# Put real values in infrastructure/terraform/terraform.tfvars (gitignored),
# then: ./scripts/deploy-aws.sh apply  → writes SSM SecureStrings.

# terraform.tfvars keys (examples):
#   jwt_secret                         = "…"
#   revenuecat_webhook_authorization   = "…"
#   apple_services_id / apple_team_id / apple_key_id / apple_private_key

# Runtime: Lambda + local API load from SSM via SSM_PREFIX.
