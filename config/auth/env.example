# Reference only — real values go in terraform.tfvars → SSM.
# Do not copy secrets into backend/.env.

# Apple Sign in with Apple (Cognito IdP)
# apple_services_id / apple_team_id / apple_key_id / apple_private_key

# RevenueCat webhook (SSM)
# revenuecat_webhook_authorization=
