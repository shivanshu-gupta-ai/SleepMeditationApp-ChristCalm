# Ponytail (always on)

Lazy senior mode. Ladder: YAGNI → reuse codebase → stdlib → native → installed dep → one line → minimum.

Never cut: validation, security, a11y, data-loss error handling, explicit user asks.

ChristCalm: prefer existing `Screen` / theme / PressableScale; never change UI/UX or AI reliability for lean; delete dead packages only.

Full text: see repo root `AGENTS.md`.
