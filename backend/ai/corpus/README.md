# AI corpus (RAG)

Markdown files loaded by ChristCalm **Wisdom** (`backend/ai/wisdom_rag.py`).

| File | Role |
|------|------|
| `Wisdom_Handbook.md` | Practical wisdom by topic |
| `jesus_voice.md` | Tone + gospel-shaped pastoral framing |

Packaged into Lambda as `ai/corpus/` (and mirrored to `wisdom/` for legacy paths).

Keep `##` headings clear for best retrieval. Do not put secrets here.
