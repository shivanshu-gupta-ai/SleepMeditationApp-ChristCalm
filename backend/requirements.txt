# Local development + tests. Lambda package uses requirements-lambda.txt.
# Keep this list to packages the app and tests actually import.

fastapi==0.141.1
mangum==0.19.0
uvicorn==0.25.0
starlette==1.3.1
pydantic==2.13.4
email-validator==2.3.0
python-multipart==0.0.32
python-dotenv==1.2.2

boto3==1.43.39
botocore==1.43.39
httpx==0.28.1

# Tests (Apple Sign In JWT helpers)
PyJWT==2.13.0
cryptography==49.0.0
pytest==9.1.1
requests==2.34.2

# Transitive pin: DoS CVEs in BER/OID/REAL decoding (fixed in 0.6.4)
pyasn1==0.6.4

# Optional local quality tools
black==26.5.1
flake8==7.3.0
mypy==2.1.0
